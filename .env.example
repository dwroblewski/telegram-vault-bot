# Cloudflare credentials (create API token at https://dash.cloudflare.com/profile/api-tokens)
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
CLOUDFLARE_ACCOUNT_ID=your_account_id

# Telegram bot (get from @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_ALLOWED_USER_ID=your_telegram_user_id

# Gemini API (get from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key

# Vault path (defaults to $HOME/projects/second-brain)
VAULT_PATH=/path/to/your/obsidian/vault

# Worker URL (from Cloudflare deployment)
WORKER_URL=https://your-worker-name.your-subdomain.workers.dev

# GitHub integration (for auto-sync captures to git)
GITHUB_REPO=your-username/your-vault-repo
# GITHUB_TOKEN - set via: wrangler secret put GITHUB_TOKEN (fine-grained, needs Contents:write)

# Sync configuration
VAULT_DEPTH=3  # Max folder depth for vault aggregation (default: 3)
